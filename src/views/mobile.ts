import { BaseView } from './base';

export class MobileView extends BaseView {
    protected articlesCount: number = 1;
    protected measurementsCount: number = 1;
    protected typeName: string = 'mobile';
}
