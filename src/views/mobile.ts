import { View } from './index';

export class MobileView extends View {
    protected nameClass: string = 'mobile';
    protected recordNumberNews: number = 1;
    protected recordNumberWeather: number = 1;
}
