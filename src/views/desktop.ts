import { GeneralCode } from './general';

export class DesktopView extends GeneralCode {
    protected lastNewsCount: number = 3;
    protected latestWeatherCount: number = 2;
    protected typeDevice: string = 'desktop';
}
